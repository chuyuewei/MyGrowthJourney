# 👑 Project Leviathan V4 — Patch 功能总览索引

**总计：315 Patches | 基于 Paper/Purpur | JDK 25 | 500+ Players**

---

## 📋 总览速查表

```
卷号    Patch范围    主题                        Patch数
──────────────────────────────────────────────────────────
第一卷  0001-0045   JVM基建与内存优化              45
第二卷  0046-0090   存储引擎                       45
第三卷  0091-0130   网络层重构                     40
第四卷  0131-0160   弱网对抗与流控                 30
第五卷  0161-0210   物理引擎与实体手术             50
第六卷  0211-0235   世界生成                       25
第七卷  0236-0265   多线程架构                     30
第八卷  0266-0315   API兼容层与Purpur调控          50
──────────────────────────────────────────────────────────
合计                                               315
```

---

## 🌌 第一卷：JVM基建与内存优化
### `Patch 0001 - 0045`

---

### Patch 0001 - 0005
**主题：ZGC 接管 GC 体系**

```
0001  替换默认GC：从G1GC切换到分代ZGC
      → 修改服务端启动脚本模板
      → 添加ZGC基础参数注入

0002  ZGC 参数调优层
      → ZUncommitDelay / ZCollectionInterval 精细化配置
      → 针对500人服的堆内存分配策略（12-24GB区间）

0003  GC 日志与监控接入
      → 结构化GC日志输出（JSON格式）
      → 暴露GC停顿时间到 /leviathan stats 命令

0004  显式GC调用拦截
      → 拦截插件调用 System.gc() 的行为
      → 替换为ZGC友好的提示性日志警告

0005  GC 压力基准测试工具
      → 内置轻量级压力探针
      → 500人负载下自动采样堆分配速率
      → 输出到 logs/gc-profile.log
```

---

### Patch 0006 - 0012
**主题：JIT 标量替换与逃逸分析强化**

```
0006  逃逸分析白名单注册
      → 标记 BlockPos / Vec3 / ChunkPos 为逃逸分析候选
      → 添加 @jdk.internal.vm.annotation.ForceInline 关键路径

0007  MutableBlockPos 对象池（ThreadLocal版）
      → 每个服务器线程持有独立MutableBlockPos池
      → 池大小：64个/线程，超出自动扩容

0008  BlockPos 热路径重写
      → 识别调用频率Top20的BlockPos构造场景
      → 改写为复用池对象的模式

0009  Vec3 / Vec3i 热路径重写
      → 同上，针对Vec3的物理计算热路径
      → 添加零向量、单位向量静态常量复用

0010  ChunkPos 热路径重写
      → 区块坐标计算路径复用
      → Long编码/解码内联优化

0011  value class 试验性替换（ChunkPos）
      → 仅对ChunkPos启用JDK25 value record
      → 不参与继承链，安全替换
      → Preview API 开关保护

0012  value class 试验性替换（Vec3i子集）
      → 对不涉及序列化的内部Vec3i实现替换
      → 兼容性测试矩阵验证
```

---

### Patch 0013 - 0020
**主题：Fastutil 全域渗透**

```
0013  Fastutil 依赖集成
      → 将 fastutil 8.5.x 打包进核心
      → 避免与插件的fastutil版本冲突（shading）

0014  EntityTracker 集合替换
      → Entity ID → Entity 映射
      → 替换为 Int2ObjectOpenHashMap<Entity>

0015  ChunkHolder 映射表替换
      → Long编码ChunkPos → ChunkHolder 映射
      → 替换为 Long2ObjectOpenHashMap<ChunkHolder>

0016  AttributeMap 内部存储替换
      → 实体属性 → 数值 映射
      → 替换为 Reference2DoubleOpenHashMap

0017  PlayerList 内部集合替换
      → 在线玩家列表、UUID查找表
      → 替换为 Reference2ReferenceOpenHashMap

0018  世界实体集合替换
      → 每个World的实体存储集合
      → 替换为 Reference2ReferenceOpenHashMap
      → 保持Bukkit API视图层兼容

0019  区块加载队列集合替换
      → 待加载/卸载区块队列
      → 替换为 LongArrayFIFOQueue

0020  Fastutil 集合 Bukkit视图包装层
      → 为所有替换的集合提供只读视图包装
      → 确保 getEntities() 等API返回类型兼容
```

---

### Patch 0021 - 0030
**主题：FerriteCore 内存去重体系**

```
0021  BlockState 属性字符串全局intern池
      → 所有BlockState属性名/值强制intern
      → 预计节省：80-120MB

0022  BlockState 对象全局唯一化
      → 确保相同BlockState只有一个实例
      → 通过规范化Map强制单例

0023  VoxelShape 碰撞箱去重
      → 相同形状的VoxelShape全局复用
      → 预计节省：150-250MB

0024  VoxelShape 计算结果缓存
      → 高频碰撞箱运算结果LRU缓存
      → 缓存大小：4096条目

0025  NBT 常用标签字符串去重
      → 高频NBT key（"id","Count","Damage"等）intern
      → 预计节省：50-100MB

0026  ItemStack 元数据去重
      → 相同NBT的ItemStack共享NBT实例
      → 写时复制（Copy-on-Write）保证安全性

0027  Biome 注册表对象去重
      → 生物群系属性对象全局复用
      → 针对大地图多生物群系场景

0028  RegistryEntry 包装去重
      → 注册表条目包装对象统一化
      → 避免重复包装同一条目

0029  内存去重统计暴露
      → /leviathan memory 命令显示去重节省量
      → 实时显示各模块节省的内存字节数

0030  去重体系压力测试
      → 启动时对去重池进行完整性校验
      → 检测潜在的对象泄漏
```

---

### Patch 0031 - 0045
**主题：JVM 参数注入与运行时诊断**

```
0031  启动参数自动校验
      → 检测JDK版本是否≥25
      → 检测--enable-preview是否开启
      → 缺失时输出明确错误并拒绝启动

0032  SIMD 能力自动探测
      → 启动时检测CPU的AVX2/AVX-512支持
      → 将结果写入运行时能力标志位
      → 供后续SIMD模块选择向量宽度

0033  虚拟线程调度器参数注入
      → 根据物理CPU核心数自动计算
      → parallelism = 物理核心数 × 2
      → maxPoolSize = parallelism × 16

0034  堆外内存（Direct Memory）限制配置
      → 为mmap/FFM操作配置堆外内存上限
      → 防止堆外内存无限增长

0035  运行时内存快照工具
      → /leviathan heapdump 触发轻量快照
      → 输出各模块内存占用分布

0036  对象分配速率监控
      → 利用JFR（Java Flight Recorder）采样
      → 每分钟输出Top10分配热点到日志

0037  JIT编译状态监控
      → 检测关键方法是否已被JIT编译
      → /leviathan jit-status 命令

0038  GC根引用分析
      → 定期扫描超大对象（>1MB）
      → 报告可能的内存泄漏候选

0039  Preview API 稳定性守护
      → 捕获Preview API的运行时警告
      → 统一输出到专用日志避免污染控制台

0040  内存压力自适应降级
      → 当堆使用率>85%时自动触发降级
      → 降级内容：关闭非必要缓存、降低预读

0041  类加载优化
      → 提前加载高频使用的类到内存
      → 减少运行时类加载停顿

0042  字符串常量池优化
      → 扩大StringTable大小
      → 针对大量方块名/物品名的场景

0043  编译器Hint注入
      → 对关键循环添加@HotSpotIntrinsicCandidate等效标注
      → 引导JIT对热路径做更激进优化

0044  运行时配置热重载
      → 支持 /leviathan reload 不重启更新配置
      → 仅对运行时安全的参数生效

0045  第一卷完整性自检
      → 服务器启动时对第一卷所有模块做自检
      → 输出启动报告：各模块状态/预期收益
```

---

## 💽 第二卷：存储引擎
### `Patch 0046 - 0090`

---

### Patch 0046 - 0055
**主题：Linear V2 存储格式**

```
0046  Anvil → Linear V2 格式转换器
      → 提供一键迁移工具（离线运行）
      → 支持断点续传（大地图迁移）

0047  Linear V2 区块写入引擎
      → 线性连续写入，消除Anvil碎片化
      → 写入缓冲区：每Region文件独立缓冲

0048  Linear V2 区块读取引擎
      → 基于偏移表的O(1)区块定位
      → 支持并发读取同一Region文件

0049  Linear V2 Region文件管理
      → Region文件生命周期管理
      → 自动回收空Region文件

0050  Linear V2 完整性校验
      → 每个区块存储CRC32校验值
      → 启动时对已有文件做抽样校验（10%）

0051  Linear V2 备份快照
      → /leviathan backup 触发原子快照
      → 利用文件系统硬链接实现零拷贝备份

0052  Linear V2 与Anvil双格式兼容读取
      → 迁移期间可同时读取新旧两种格式
      → 保证热迁移不丢数据

0053  Linear V2 写入队列
      → 所有写入操作进入异步队列
      → 主线程永不阻塞于写入

0054  Linear V2 写入限速
      → 避免保存风暴导致磁盘I/O饱和
      → 可配置最大写入速率（MB/s）

0055  Linear V2 统计暴露
      → 记录读/写次数、平均延迟、缓存命中率
      → /leviathan storage stats 命令
```

---

### Patch 0056 - 0065
**主题：Zstd 字典压缩引擎**

```
0056  Zstd 库集成
      → 集成 zstd-jni，替换 zlib
      → 针对JDK25的本地库加载优化

0057  Zstd 压缩级别配置
      → 默认Level 3（平衡速度与压缩率）
      → 提供Level 1-9的配置选项

0058  字典训练器（后台线程）
      → 从已有区块随机采样1000个作为训练集
      → 训练线程优先级：MIN_PRIORITY
      → 首次训练：启动后3分钟触发

0059  字典热更新机制
      → 新探索区块超已有总量10%时重训
      → 重训期间无缝使用旧字典
      → 新字典就绪后原子切换

0060  字典版本管理
      → 每个字典分配版本号
      → 区块元数据记录压缩时使用的字典版本
      → 解压时自动选择对应版本字典

0061  多字典并行支持
      → 为主世界/下界/末地分别训练专属字典
      → 各维度数据特征不同，专属字典效果更佳

0062  压缩率统计
      → 实时记录各维度的平均压缩率
      → 与理论值对比（预期50-55%）

0063  压缩流水线优化
      → 区块序列化与压缩流水线化
      → 避免等待，提高吞吐

0064  解压缓存
      → 热点区块解压结果短暂缓存（LRU 256条目）
      → 避免重复解压同一区块

0065  Zstd 降级保护
      → 压缩失败时自动降级到zlib
      → 记录失败原因，不丢数据
```

---

### Patch 0066 - 0075
**主题：FFM API + mmap 零拷贝I/O**

```
0066  FFM API 初始化层
      → JDK25 Panama FFM API 接入
      → Arena生命周期管理封装

0067  mmap 区块文件映射
      → 将Region文件映射到堆外内存
      → 按需映射（非全量），控制内存占用

0068  页面预读策略
      → spawn区域500格：启动时主动预读
      → 玩家活跃区：异步提前预读相邻区块
      → 降低Page Fault概率

0069  OS内核参数推荐输出
      → 启动时检测当前sysctl参数
      → 不符合推荐值时输出警告与建议命令

0070  堆外内存上限守护
      → 监控mmap占用的堆外内存总量
      → 超出配置上限时触发LRU淘汰

0071  mmap 与Linear V2集成
      → mmap读取直接对接Linear V2解析器
      → 零中间拷贝：磁盘→页缓存→解析器

0072  写入路径：异步msync
      → 脏页刷盘使用异步msync
      → 主线程不等待刷盘完成

0073  文件描述符池
      → 复用Region文件的FileChannel
      → 避免频繁open/close系统调用

0074  I/O 延迟分布监控
      → 记录每次读取的实际延迟
      → 统计P50/P95/P99延迟
      → 暴露到 /leviathan storage latency

0075  冷启动加速
      → 记录上次运行的热点区块列表
      → 下次启动时优先预读这些区块
```

---

### Patch 0076 - 0090
**主题：玩家数据 RocksDB 化**

```
0076  RocksDB JNI 集成
      → 集成 rocksdbjni
      → 针对Linux平台的本地库优化配置

0077  RocksDB 实例初始化
      → 配置LSM树参数（针对500人读写特征）
      → WAL日志开启，防止数据丢失

0078  playerdata NBT → RocksDB 迁移
      → Key: UUID (16字节二进制)
      → Value: 压缩后的NBT字节数组
      → 首次启动自动迁移，进度条显示

0079  advancements JSON → RocksDB 迁移
      → Key: UUID + ":adv" 后缀
      → Value: Zstd压缩的JSON字节数组

0080  stats JSON → RocksDB 迁移
      → Key: UUID + ":stats" 后缀
      → 统计数据迁移

0081  玩家数据异步读取
      → 玩家登录时虚拟线程异步读取数据
      → 主线程不阻塞，数据就绪后回调

0082  玩家数据异步写入
      → 玩家退出/自动保存时异步写入
      → 写入队列保证顺序，防止脏写

0083  RocksDB 批量写入优化
      → 自动保存时使用WriteBatch批量提交
      → 减少FSSync次数

0084  玩家数据导出工具
      → /leviathan export-playerdata <uuid>
      → 从RocksDB导出为标准NBT文件

0085  玩家数据导入工具
      → 支持从原始NBT文件导入到RocksDB
      → 用于跨服迁移或数据恢复

0086  RocksDB 压实（Compaction）调度
      → 在服务器低峰期（凌晨）触发压实
      → 防止LSM层级过多影响读性能

0087  RocksDB 监控指标
      → 读/写延迟、缓存命中率、磁盘占用
      → /leviathan playerdata stats

0088  H2数据库备选方案
      → 提供H2 WAL模式的完整实现
      → leviathan.yml切换backend无需迁移工具

0089  SQLite WAL备选方案
      → 提供SQLite WAL模式实现（≤200人服）
      → 最简单的运维方案

0090  数据库健康检查
      → 启动时验证数据库完整性
      → 检测损坏记录并尝试从备份恢复
```

---

## 🕸️ 第三卷：网络层重构
### `Patch 0091 - 0130`

---

### Patch 0091 - 0100
**主题：底层传输层升级**

```
0091  Netty Epoll 强制开启
      → Linux下强制使用EpollEventLoopGroup
      → 替换NioEventLoopGroup
      → 添加运行时检测与回退

0092  Netty EventLoop 线程数配置
      → 根据CPU核心数自动计算Worker线程数
      → 推荐：物理核心数 / 2

0093  Netty io_uring 试验性接入
      → 集成 netty-incubator-transport-io_uring
      → 通过配置开关控制启用
      → 自动检测内核版本（需5.10+）

0094  TCP 参数优化
      → TCP_NODELAY 强制开启（降低延迟）
      → SO_RCVBUF / SO_SNDBUF 调优
      → TCP_KEEPALIVE 配置

0095  连接限流器
      → 单IP每秒最大连接数限制
      → 防止连接风暴攻击

0096  数据包接收速率限制
      → 每个连接的入站数据包速率上限
      → 超出时断开连接并记录日志

0097  网络线程亲和性绑定
      → 将Netty Worker线程绑定到特定CPU核心
      → 减少跨核缓存失效

0098  ChannelPipeline 优化
      → 精简不必要的Handler
      → 减少数据包处理链路长度

0099  网络层异常统一处理
      → 捕获所有网络异常并分类处理
      → 区分：正常断线/超时/恶意断开

0100  网络层监控基础设施
      → 每秒记录：收包数/发包数/带宽/连接数
      → /leviathan network stats 命令
```

---

### Patch 0101 - 0115
**主题：Zstd 网络压缩层**

```
0101  网络压缩算法替换（Zlib → Zstd）
      → 替换数据包压缩的Zlib实现
      → 客户端兼容性：通过协议层转换保持兼容

0102  压缩阈值动态调整
      → 根据当前服务器负载动态调整压缩阈值
      → 高负载时提高阈值（少压缩），减少CPU占用

0103  区块数据包专用压缩池
      → 为区块数据包创建独立的Zstd压缩上下文池
      → 避免上下文重建开销

0104  压缩任务异步队列
      → 区块压缩任务提交到有界虚拟线程池
      → 池大小上限：128并发任务
      → 背压控制：Semaphore(256)

0105  解压任务异步队列
      → 入站数据包解压同样异步化
      → 完成后回调Netty Pipeline继续处理

0106  压缩级别自适应
      → 监控压缩任务队列深度
      → 队列积压时自动降低压缩级别（速度优先）

0107  小包跳过压缩
      → 低于128字节的数据包跳过压缩
      → 避免压缩头开销大于数据本身

0108  压缩统计
      → 记录各类数据包的压缩前后大小
      → 计算实时压缩率

0109  网络Zstd字典（进阶）
      → 为高频数据包类型训练专属压缩字典
      → 如：ChunkData包、EntityMove包

0110  压缩降级保护
      → 压缩失败时发送未压缩数据并记录警告
      → 不因压缩错误断开玩家连接

0111  客户端能力协商
      → 握手阶段协商压缩算法
      → 不支持Zstd的客户端降级到Zlib

0112  带宽峰值平滑
      → 检测单tick发送量峰值
      → 超出阈值时将部分包推迟到下一tick

0113  出站队列优先级
      → 为每个连接维护优先级出站队列
      → 战斗包优先于区块包发送

0114  入站数据包速率统计
      → 记录每玩家每秒接收的数据包量
      → 用于后续反作弊分析

0115  网络压缩层完整性测试
      → 启动时发送测试数据包验证压缩/解压正确性
```

---

### Patch 0116 - 0130
**主题：数据包序列化优化**

```
0116  数据包对象池
      → 复用高频数据包对象（EntityMove/ChunkData）
      → 避免每次发包重新分配

0117  ByteBuf 池化优化
      → 使用Netty的PooledByteBufAllocator
      → 针对500人并发的池大小调优

0118  数据包批量合并
      → 同一tick内发给同一玩家的小包合并发送
      → 减少系统调用次数

0119  实体移动包插值压缩
      → 对连续的实体移动包做差分编码
      → 只发送位置增量而非绝对坐标

0120  区块数据包分片发送
      → 超大区块数据包分割为多个小片
      → 避免单包阻塞整个连接

0121  数据包发送窗口控制
      → 基于RTT动态调整发送窗口大小
      → 防止缓冲区溢出

0122  空包过滤
      → 检测并丢弃内容为空的数据包
      → 如：0实体更新的EntityData包

0123  重复包去重
      → 短时间内内容相同的包只发一次
      → 针对高频触发的状态同步包

0124  数据包加密优化
      → 利用AES-NI硬件加速数据包加密
      → 替换纯Java的AES实现

0125  序列化缓冲区预分配
      → 根据数据包类型预分配合适大小的缓冲区
      → 避免序列化过程中的缓冲区扩容

0126  协议版本兼容层
      → 支持多版本客户端协议
      → 通过ViaVersion集成优化

0127  数据包日志采样
      → 可配置的数据包日志采样（调试用）
      → 默认关闭，不影响生产性能

0128  序列化热路径分析
      → 启动后5分钟输出序列化耗时Top10报告

0129  数据包大小上限守护
      → 超大入站包直接断开连接
      → 防止恶意大包攻击

0130  网络层第三卷完整性验证
      → 各子模块互联测试
      → 模拟500并发连接压力测试（本地）
```

---

## 📡 第四卷：弱网对抗与流控
### `Patch 0131 - 0160`

---

### Patch 0131 - 0142
**主题：令牌桶动态限速**

```
0131  玩家网络质量实时评估
      → 基于KeepAlive包计算RTT
      → 基于重传统计计算丢包率
      → 每5秒更新一次网络质量等级

0132  网络质量分级定义
      → Tier S/A/B/C/D 五级分类
      → 各级别触发条件的精确定义

0133  令牌桶实现
      → 每玩家独立的令牌桶实例
      → 令牌生成速率与网络质量等级绑定

0134  区块发送速率控制
      → 令牌桶控制每秒发送的区块数
      → Tier S:20个/s → Tier D:0个/s

0135  战斗包豁免机制
      → Priority 0 数据包绕过令牌桶
      → 无论网络质量如何，战斗包立即发送

0136  网络质量变化平滑过渡
      → Tier变化时速率平滑过渡（非突变）
      → 避免玩家感受到明显的加载停顿

0137  弱网玩家提示
      → 检测到Tier C/D时向玩家发送提示消息
      → 可配置是否显示

0138  令牌桶参数可配置
      → 各Tier的区块发送速率均可在配置文件调整
      → 支持运行时热重载

0139  全服网络质量统计
      → 记录各Tier玩家占比
      → /leviathan network quality 命令

0140  弱网玩家自动降低视距
      → Tier C/D玩家自动减少服务端视距
      → 减少需要发送的区块总量

0141  弱网超时保护
      → Tier D持续30秒 → 触发友好断线
      → 断线原因写入日志

0142  令牌桶压力测试
      → 模拟弱网场景验证令牌桶行为
```

---

### Patch 0143 - 0160
**主题：懒加载与防重发体系**

```
0143  玩家已知区块记忆集
      → 为每个玩家维护 LongOpenHashSet
      → 记录该玩家已接收过的区块坐标

0144  记忆集大小自适应
      → 根据玩家视距动态调整记忆集大小
      → 基础大小：视距² × 1.5

0145  区块重发抑制
      → 玩家请求已在记忆集中的区块时拒绝重发
      → 仅发送该区块自上次发送后的变化

0146  区块变化差分追踪
      → 记录每个区块自上次发送后发生变化的方块
      → 仅发送变化的方块数据（非整个区块）

0147  边界横跳检测
      → 检测玩家在区块边界来回移动的行为
      → 触发冷却机制，禁止重复请求同一区块

0148  区块卸载记忆集清理
      → 玩家卸载某区块时从记忆集移除
      → 避免记忆集无限增长

0149  跨服区块记忆同步
      → 为BungeeCord/Velocity环境保存记忆集
      → 玩家跨服返回时不重发已知区块

0150  实体更新懒发送
      → 玩家已知区块内的实体，仅发送变化
      → 静止实体停止发送位置更新包

0151  区块优先级队列
      → 按玩家视角方向优先发送正前方区块
      → 背后的区块延迟发送

0152  区块发送顺序优化
      → 螺旋形由近到远的发送顺序
      → 玩家最快看到正前方的地形

0153  区块预加载预测
      → 根据玩家移动方向预测下一秒位置
      → 提前触发目标区块的加载

0154  区块卸载延迟
      → 玩家离开区块后不立即卸载
      → 延迟10秒，应对玩家快速折返

0155  带宽使用量统计
      → 记录每玩家每分钟的区块数据流量
      → 全服带宽使用趋势图（/leviathan bw）

0156  区块发送失败重试
      → 发送失败的区块加入重试队列
      → 最多重试3次，超出记录警告

0157  恶意区块请求防护
      → 检测异常的区块请求频率
      → 触发临时封禁并记录到安全日志

0158  视距动态调整
      → 根据全服TPS自动调整服务端视距
      → TPS < 15 → 视距-1；TPS > 19 → 视距+1

0159  区块发送统计报告
      → 每小时输出区块发送效率报告
      → 包含缓存命中率、节省的带宽量

0160  第四卷完整性验证
      → 模拟边界横跳场景的功能测试
```

---

## ⚔️ 第五卷：物理引擎与实体手术
### `Patch 0161 - 0210`

---

### Patch 0161 - 0175
**主题：DAB 动态脑叶激活**

```
0161  DAB 框架基础
      → 实体距离检测基础设施
      → 最近玩家快速查找（空间索引）

0162  距离-频率映射表
      → 5级距离分段的Tick频率配置
      → 支持运行时热更新

0163  实体Tick调度器改造
      → 在实体Tick入口注入频率检查
      → 零开销跳过（非活跃帧直接return）

0164  Boss实体豁免
      → 凋零/末影龙/劫掠兽永远全频率
      → 豁免列表可配置扩展

0165  战斗状态检测
      → 检测实体是否有攻击目标且在范围内
      → 战斗中的实体豁免降频

0166  骑乘实体豁免
      → 被玩家骑乘的实体保持全频率
      → 包括：马、猪、船（含实体）

0167  DAB 与寻路引擎联动
      → 降频实体同步降低寻路更新频率
      → 1/8频率实体：寻路每8tick更新一次

0168  DAB 与动画联动
      → 降频实体跳过非关键动画帧
      → 客户端插值补偿（不影响视觉）

0169  DAB 统计监控
      → 实时统计各频率等级的实体数量
      → /leviathan entity dab-stats

0170  DAB 预热
      → 服务器启动后2分钟DAB全面生效
      → 避免启动阶段频繁重算距离

0171  动态实体密度检测
      → 检测单区块实体密度
      → 高密度区域整体降频（群体优化）

0172  群体AI共享寻路
      → 相同类型且密集的实体共享寻路结果
      → 节省90%高密度场景的寻路计算

0173  实体剔除（超出范围）
      → 距所有玩家超过128格的实体进入超级休眠
      → 仅保留基本状态，完全跳出Tick循环

0174  超级休眠唤醒
      → 玩家接近128格时唤醒超级休眠实体
      → 唤醒时恢复完整状态

0175  DAB 完整性保证
      → 确保降频不影响实体最终状态正确性
      → 关键状态变更（死亡/掉落物）强制立即处理
```

---

### Patch 0176 - 0185
**主题：SIMD 向量化计算**

```
0176  Vector API 初始化层
      → 根据CPU能力选择向量宽度
      → AVX2: SPECIES_256 / AVX-512: SPECIES_512

0177  批量实体距离计算（SIMD）
      → 8/16路并行计算实体与玩家距离
      → 用于DAB的距离判断热路径

0178  批量视线检测（SIMD）
      → 8/16路并行计算射线方向向量
      → 用于实体视野检测

0179  批量AABB碰撞检测（SIMD）
      → 并行计算多个轴对齐包围盒的相交
      → 用于实体碰撞粗检测阶段

0180  光照计算向量化
      → 批量计算方块光照值
      → 光照更新时的传播计算向量化

0181  噪音采样向量化
      → 地形生成时的噪音函数批量采样
      → 提升区块生成速度

0182  标量降级实现
      → 所有SIMD操作提供标量等价实现
      → 无AVX支持时自动降级

0183  SIMD 结果验证工具
      → 开发模式下对比SIMD与标量结果
      → 确保向量化不引入计算误差

0184  SIMD 性能基准
      → 启动时对SIMD模块进行微基准测试
      → 输出实际加速比到日志

0185  SIMD 模块热路径分析
      → JFR采样SIMD模块的实际执行时间
      → 识别向量化效果不理想的路径
```

---

### Patch 0186 - 0200
**主题：实体AI手术**

```
0186  受困村民检测
      → 检测村民1x1空间受困状态
      → 算法：尝试寻路失败次数阈值

0187  受困村民AI切断
      → 切断寻路引擎（A*完全停止）
      → 切断POI兴趣点扫描
      → 保留：交易响应、繁殖逻辑

0188  受困村民定期解困检测
      → 每30秒重新检测是否仍受困
      → 解困后立即恢复完整AI

0189  自由村民POI扫描降频
      → 非受困村民POI扫描频率降至1/40
      → 对玩家体验无感知影响

0190  村民工作站缓存
      → 缓存村民已知的工作站位置
      → 避免每次都重新扫描

0191  漏斗休眠条件检测
      → 检测：上方无物品 且 目标容器已满
      → 满足条件时触发深度休眠

0192  漏斗深度休眠实现
      → 完全移出Tick循环
      → 注册方块更新监听器作为唤醒触发

0193  漏斗唤醒机制
      → 上方出现物品时立即唤醒
      → 目标容器有空位时立即唤醒

0194  漏斗休眠统计
      → 记录当前休眠漏斗数量
      → /leviathan entity hopper-stats

0195  掉落物管理：合并优化
      → 0.5格内相同物品自动合并
      → 合并时保留最早的生命周期

0196  掉落物管理：老化加速
      → 存在5分钟以上的掉落物，合并半径扩大到2格
      → 加速清理古老掉落物

0197  掉落物管理：区块上限
      → 单区块掉落物超100个时触发警告
      → 超200个时强制加速消失（可配置）

0198  经验球合并优化
      → 相邻经验球自动合并为大球
      → 减少经验球实体数量

0199  箭矢实体优化
      → 插在地上的箭矢降至1/20 Tick频率
      → 超时后自动消失计时

0200  AI手术统计总览
      → /leviathan entity ai-stats
      → 显示各优化模块节省的Tick计算量
```

---

### Patch 0201 - 0210
**主题：物理熔断与碰撞优化**

```
0201  实体堆叠密度检测
      → 实时计算每个1x1格的实体数量
      → 使用空间哈希表加速统计

0202  碰撞熔断机制
      → 超过阈值时跳过当前Tick的物理推挤
      → 正常阈值：5个/格
      → PVP模式阈值：10个/格

0203  PVP场景自动检测
      → 检测玩家间的攻击事件
      → 周围32格内有PVP时提升碰撞阈值

0204  流体物理优化
      → 静止流体（水/岩浆）完全跳出Tick
      → 仅边界方块保持活跃

0205  流体唤醒机制
      → 相邻方块变化时唤醒相关流体
      → 避免流体错误凝固

0206  TNT实体批量处理
      → 多个TNT同时引爆时合并爆炸计算
      → 减少爆炸物理计算的重复部分

0207  爆炸伤害计算优化
      → 使用空间索引加速爆炸范围内实体查找
      → 替换原版的暴力遍历

0208  活塞物理优化
      → 活塞推动时的方块移动计算缓存
      → 相同配置的活塞动作结果复用

0209  重力方块优化（沙/砂砾）
      → 批量处理同列的重力方块
      → 避免逐个下落的O(N)延迟链

0210  物理引擎第五卷完整性验证
      → 验证熔断不影响游戏规则正确性
      → 特别测试：TNT/流体/活塞边界情况
```

---

## 🌍 第六卷：世界生成
### `Patch 0211 - 0235`

---

### Patch 0211 - 0220
**主题：异步区块生成**

```
0211  区块生成线程池
      → 独立于主线程的区块生成线程池
      → 线程数：物理核心数 / 2（不抢主线程）

0212  区块生成任务队列
      → 优先级队列：玩家附近 > 预生成
      → 防止预生成任务饿死玩家触发的生成

0213  区块生成结果回调
      → 生成完成后通过主线程任务队列回调
      → 线程安全的区块注册

0214  区块生成限速
      → 限制每秒最大生成区块数
      → 防止生成风暴压垮CPU

0215  预生成引擎
      → /leviathan pregen <半径> 触发预生成
      → 支持暂停/恢复
      → 进度持久化（重启后继续）

0216  预生成进度显示
      → 实时显示预生成进度（%/ETA）
      → 支持多世界并发预生成

0217  区块生成缓存
      → 相同噪音参数下的生物群系结果缓存
      → 加速相邻区块的生成

0218  噪音函数并行化
      → 单个区块内的噪音采样并行执行
      → 利用第五卷的SIMD向量化

0219  区块生成统计
      → 记录平均生成耗时、峰值耗时
      → 按生物群系分类统计

0220  区块生成错误恢复
      → 生成失败时记录错误并生成空区块
      → 不因单个区块生成失败影响服务器
```

---

### Patch 0221 - 0235
**主题：史诗地形（可选模块）**

```
0221  史诗地形模块开关
      → 独立的启用/禁用开关
      → 禁用时完全无性能影响

0222  山峰振幅调整
      → 密度函数振幅上限 × 1.2（修正后）
      → 参数可在配置文件精细调整

0223  峡谷侵蚀系数调整
      → 侵蚀参数 × 1.3
      → 分别针对大陆/海洋地形

0224  洞穴阈值扩展
      → 3D洞穴噪音阈值扩大10%（修正后）
      → 生成更宽敞的地下空间

0225  384格高度完整支持
      → 确保整个高度范围的地形合理性
      → 修复超高山峰的悬浮地形问题

0226  史诗地形与生物群系协调
      → 确保史诗地形不破坏生物群系分布
      → 山顶强制积雪/冻土处理

0227  史诗地形性能影响评估
      → 对比默认地形与史诗地形的生成耗时
      → 建议配合Patch 0215预生成使用

0228  结构生成适配
      → 确保村庄/要塞/古城在史诗地形中正确生成
      → 处理悬崖/峡谷导致的结构破坏问题

0229  防种子破解模块（独立可选）
      → 与史诗地形完全解耦
      → 仅修改矿物/结构的坐标噪音

0230  ChaCha8 流加密坐标扰动
      → 使用ChaCha8对区块坐标加密
      → 生成不可反推的矿物分布

0231  SHA3-256 种子派生
      → 从主种子派生各维度子种子
      → 增加暴力破解难度

0232  防破解对游戏体验的透明性
      → 验证矿物总量与原版一致
      → 仅分布位置改变，不影响资源量

0233  世界生成基准测试
      → 自动化测试：生成1000个区块
      → 对比性能数据与预期值

0234  地形预览工具
      → /leviathan terrain-preview <x> <z>
      → 输出该坐标的地形参数快照

0235  第六卷完整性验证
      → 生成测试世界并验证所有结构正确生成
```

---

## ⏳ 第七卷：多线程架构
### `Patch 0236 - 0265`

---

### Patch 0236 - 0250
**主题：Phase 1 异步卸载层**

```
0236  主线程负载监控
      → 实时采样主线程每个阶段的耗时
      → 识别最重的Top5操作

0237  异步区块保存队列
      → 区块数据序列化移出主线程
      → 主线程仅提交保存任务，不等待完成

0238  异步区块加载调度
      → 区块读取与反序列化在后台线程完成
      → 完成后的注册仍在主线程（线程安全）

0239  异步玩家数据读写
      → 登录时虚拟线程读取RocksDB
      → 登出时虚拟线程写入RocksDB

0240  异步插件文件I/O拦截
      → Patch 0339（第八卷）的基础设施层
      → 提供异步I/O重定向的核心API

0241  Tick任务分类器
      → 将主线程Tick中的任务分类
      → 分类：必须同步/可异步/可延迟

0242  可延迟任务队列
      → 非紧急的Tick任务推入延迟队列
      → 在Tick有空闲时间时执行

0243  主线程看门狗增强
      → 检测主线程卡顿超过100ms
      → 输出当前Tick的任务栈快照

0244  线程池生命周期管理
      → 统一管理所有后台线程池
      → 优雅关闭（shutdown时等待队列清空）

0245  跨线程任务调度API
      → 提供类型安全的跨线程任务提交接口
      → 为第八卷插件API兼容层铺路

0246  异步任务异常处理
      → 后台任务失败时不影响主线程
      → 失败任务记录到专用日志

0247  线程亲和性配置
      → 允许配置哪些核心用于哪类任务
      → 如：核心0-3专用于Netty，4-15用于计算

0248  主线程CPU使用率监控
      → 实时监控主线程CPU占用
      → /leviathan perf thread-stats

0249  异步层压力测试
      → 模拟500人同时登录的数据加载压力
      → 验证无数据竞争/丢失

0250  Phase 1 收益评估报告
      → 运行24小时后输出阶段性报告
      → 主线程负载降低量、GC改善量
```

---

### Patch 0251 - 0265
**主题：Phase 2 区域化Tick基础**

```
0251  Region划分算法
      → 基于玩家分布动态划分Region
      → 相邻玩家归入同一Region

0252  Region生命周期管理
      → Region的创建、合并、分裂逻辑
      → 避免Region数量爆炸

0253  Region内Tick独立化
      → 无玩家依赖区域的Tick完全独立
      → 不依赖主线程调度

0254  Region间数据同步协议
      → 定义Region边界数据的同步格式
      → 每Tick结束时同步边界状态

0255  双缓冲状态快照
      → 边界玩家读取上一Tick的状态快照
      → 1Tick延迟换取无锁访问

0256  实体跨Region迁移
      → 实体移动跨越Reg
